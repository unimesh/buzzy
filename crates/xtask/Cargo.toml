[package]
name = "xtask"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Repo automation: the single source of truth for what 'green' means (fmt, clippy, test, arch checks)."
publish = false

[lints]
workspace = true

[dev-dependencies]
cargo_metadata = "0.18"
